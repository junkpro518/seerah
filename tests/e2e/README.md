# اختبارات E2E (Playwright)

المسارات الحرجة (الدخول/2FA، تحرير معلومة موثّقة، انتقال مراجعة) تُضاف ضمن أطوارها (US1/US2/US4).

**تعمل على مكدّس Supabase محلي زائل فقط** (`supabase start` + `supabase db reset`) — لا تمسّ السحابي إطلاقًا (مبدأ VI).
